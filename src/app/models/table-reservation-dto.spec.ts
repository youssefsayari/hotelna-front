import { TableReservationDTO } from './table-reservation-dto';

describe('TableReservationDTO', () => {
  it('should create an instance', () => {
    expect(new TableReservationDTO()).toBeTruthy();
  });
});
